# App
SECRET_KEY=replace-with-a-strong-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Dev default DB (SQLite). For production, switch to Postgres:
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/algo
DATABASE_URL=sqlite:///./app.db

# Broker: "mt5" or "zerodha"
BROKER=mt5

# MT5 (supports demo or live as per your account)
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
# Optional: path to terminal if auto-detect fails (Windows example)
# MT5_TERMINAL_PATH=C:\\Program Files\\MetaTrader 5\\terminal64.exe
MT5_DEVIATION=10

# Zerodha (no official paper trading; requires live API credentials)
ZERODHA_API_KEY=
ZERODHA_API_SECRET=
# Generate access token via login flow and paste here
ZERODHA_ACCESS_TOKEN=
# Default product for Zerodha orders: MIS (intraday) or NRML (overnight)
ZERODHA_DEFAULT_PRODUCT=MIS